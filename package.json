{
  "name": "learn-playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tests:api": "playwright test --config=api.config.ts --project=Chromium",
    "tests:apireporter": "playwright test --config=api.config.ts --project=Chromium --reporter mochawesome",
    "tests:html": "playwright test --config=api.config.ts --reporter mochawesome",
    "pretest": "tsc --incremental -p tests/tsconfig.json",
    "test": "playwright test -c tests-out"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.34.3",
    "mochawesome": "^7.1.3"
  }
}
